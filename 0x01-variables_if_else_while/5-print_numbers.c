#include <stdio.h>

/**
* main - Entry point
*
* This code prints all single digit numbers of base 10 starting from 0
*
* Return: Always 0 (Successful)
**/

int main(void)
{
	int i = 0;

	for (i = 0; i < 10; i++)
	{printf("%d", i); }
	printf("\n");
	return (0);
}
