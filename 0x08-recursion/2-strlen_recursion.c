#include "main.h"
#include <string.h>
/**
* _strlen_recursion - Entry point & function name
*
* @s: pointer name
*
* This function returns the lenght of a string
*
* Return: Always 0 (success)
**/
int _strlen_recursion(char *s)
{
	int len;

	len = strlen(s);

	return (len);
}
